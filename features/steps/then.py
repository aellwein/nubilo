from behave import then


@then("database file is created with default settings")
def database_file_is_created_with_default_settings(context):
    assert True is False


@then("entry page is redirected for the first login")
def login_page_is_redirected_for_the_first_login(context):
    assert True is False


@then("login page is displayed")
def login_page_is_displayed(context):
    assert True is False
